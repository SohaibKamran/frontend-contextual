import { FilterAdvertiserPipe } from './filter-advertiser.pipe';

describe('FilterAdvertiserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdvertiserPipe();
    expect(pipe).toBeTruthy();
  });
});
